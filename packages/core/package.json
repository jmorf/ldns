{
  "name": "@ldns/core",
  "version": "0.1.0",
  "description": "Pure-TypeScript shared core for LDNS — DNS queries, RDAP, email, server, security, ASN, PTR, TLS-from-CT.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/psl": "^1.1.3",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "psl": "^1.15.0"
  }
}
